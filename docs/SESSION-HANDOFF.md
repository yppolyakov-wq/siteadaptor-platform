# SESSION HANDOFF — точка входа следующей сессии (2026-06-29)

> Новая сессия: **прочитай этот файл первым**, затем продолжай. Отвечаем по-русски.
> **Пиши простым текстом, что делаешь на каждом шаге** (не только тул-вызовы — иначе владельцу
> кажется, что всё зависло, и он прерывает). Длинные операции запускай в ФОНЕ (`run_in_background`),
> т.к. обычные команды прерываются входящими сообщениями и результат теряется.

## Связь с CLAUDE.md (память проекта — авто-грузится)
Этот хендофф = ДЕЛЬТА текущей сессии. CLAUDE.md даёт §1-2 архитектуру (Django 5.1 multi-tenant DACH),
§5 конвенции, §6 доки, §7 БОЛЬШОЙ роадмап, §8 деплой. Текущий активный под-трек = редактор витрины +
inline-content (этот документ). После него — по CLAUDE.md §7 (M20 Site Builder, архетипы «Спринт F»,
Спринт G «анти-Битрикс» AB1-5, Stage 2/3). Source of truth по сделанному — `docs/build-log.md`.

## Ветка и цикл
- Ветка: **`claude/event-detail-section-order-qk6t76`**.
- Цикл: `uv run ruff check .` + `uv run ruff format --check .` (раздельно, ВСЯ репа) → `npm run build:css`
  при правке шаблонов (коммитить `static/css/app.css`) → `uv run pytest <модули> --reuse-db` (миграции → `--create-db`)
  → push → CI зелёный → FF-merge: `git push origin <sha>:main` → строка в `docs/build-log.md`.
- Коммиты: `Co-Authored-By: Claude <noreply@anthropic.com>` + `Claude-Session: <url>`. Идентификатор модели НЕ светить.
- CI ~12-16 мин. Статус: `mcp__github__actions_list` (отдаёт ~400КБ → в файл; парсить `jq -r '.workflow_runs[0]|"\(.head_sha[0:7]) \(.status) \(.conclusion)"' <file>`, файл удалять). AskUserQuestion здесь НЕ работает — развилки текстом.

## ГЛАВНЫЙ УРОК (почему были баги на проде)
Юнит-тесты проверяют отрендеренный HTML, **а не поведение JS** редактора. Все баги были в JS
(состояние попапа, перебивка пресета, дефолт тумблера). **Правки `templates/tenant/site_home.html`
ОБЯЗАТЕЛЬНО проверять в браузере** (Chromium + Playwright в окружении) ДО отдачи владельцу.

### Готовый тестовый стенд (поднят в этой сессии — переиспользуй)
- Тенант `baeckerei_test` → **`http://baeckerei-test.siteadaptor.de:8000/`** (`manage.py create_test_tenant`).
- Юзер **owner@test.de / test12345** (superuser, public-схема). Демо: 6 товаров + категория «Brot», секции products/promotions вкл.
- Настройка (`DJANGO_SETTINGS_MODULE=config.settings.development`): `migrate_schemas --shared`, `create_test_tenant`,
  затем `shell < scratchpad/setup2.py` (юзер/товары/форс-логин сессия в схеме тенанта, печатает SESSIONID).
- Сервер: `uv run python manage.py runserver 0.0.0.0:8000` (фон).
- Браузер БЕЗ /etc/hosts: Chromium с `--host-resolver-rules=MAP *.siteadaptor.de 127.0.0.1`; навигировать на
  `http://baeckerei-test.siteadaptor.de:8000/dashboard/site/home/`; добавить cookie `sessionid=<SESSIONID>`
  (domain `baeckerei-test.siteadaptor.de`). Скрин → владельцу через SendUserFile. Проверь middleware-блок (биллинг).

## СДЕЛАНО и в `main` (деплой: `git pull origin main && ./scripts/deploy.sh single`)
- SE-9 «редактор для ребёнка»: компактный рейл, мобильная версия (рамка телефона), правка моб-раскладки в Простом,
  медиа внутрь блоков (общая «Медиа» убрана), режим «Edit on site», динамический рейл иконок блоков.
- FIX прод-500 на `/dashboard/site/` (`site_view` падал на repeatable-блоках → guard `if key in labels`).
- FIX попап копил чужие блоки (`openBlockPopup` не ставил `popupRow`). «Edit on site» дефолт → ВЫКЛ.
- Фаза 1 inline-content: имя товара правится на карточке витрины (`product_inline_edit` + `[data-edit-model]`).
- FIX скролл меню кабинета (`#dash-nav` без `min-h-0`; сайдбар `md:sticky h-screen`).

## Точное состояние git (обновлено 2026-06-29, сессия 2 — всё в main)
ВСЁ ниже — уже в **main**, CI зелёный, FF-merge. Сессия 2 закрыла: баг D, ТЗ редактора #2/#3/#4/#6,
фикс сворачивания панели, и ВЕСЬ медиа-трек: **M1** (лого), **#7 A+B** (✎-к-форме + инлайн-цена),
**M2** (слайды баннера), **M3** (обложки разделов из билдера), **M4** (инлайн-замена фото товара).
Миграций НЕ добавляли. Медиа-трек завершён. Ветка работы: `claude/session-handoff-docs-tc1qws`.

## ОТКРЫТЫЕ ЗАДАЧИ (ТЗ владельца, по приоритету)
1. ✅ **Баг D — раскладка («2 в ряд» показывала 3).** Корень: явный пер-девайс `cols` перебивал пресет в
   `normalize_layout`; фикс — клик по миниатюре-пресете сбрасывает `cols/mobile/tablet`. Проверено в браузере
   (3→2 колонки), CI зелёный, в main `bfecd8a`.
2. ✅ **Одна фокус-панель «только выбранный блок»** (убран 2-й плавающий попап). `#bld-block-popup` перенесён в
   левую панель; `.focus-on` прячет список блоков; шапка «← Ко всем блокам». Проверено в браузере (вход через
   иконку рейла и клик секции; возврат восстанавливает список). Коммит `9ccfe46`.
3. ✅ **Крупнее иконки рейла + подписи** (`w-14→w-20`, `w-12 py-1.5→w-16 py-2`, эмодзи `text-2xl`, подписи
   `text-[11px]`). Проверено (рейл 79px/эмодзи 24px/подпись 11px). Коммит `9037a01`.
4. ✅ **Десктоп-ширина превью ≥1024px** (логический 1280px + масштаб на узкой канве) → `lg:grid-cols-N`
   срабатывают. Проверено (iframe.innerWidth=1280, lg active, «4 в ряд»→4 колонки). Коммит `9037a01`.
5. ✅ **Сворачиваемая панель — явная кнопка** — уже покрыто: ✕ в шапке панели + клик активной иконки рейла
   сворачивает. Доп. UI не добавлял.
6. ✅ **Аккордеон-группы в меню кабинета** (`_base_dashboard.html`): заголовки-кнопки, `localStorage`, активная
   развёрнута, поиск раскрывает. Проверено в браузере. Коммит `6806f48`.
7. ✅ **#7-A «✎ к форме»** (карточка → `catalog:product-edit`, скрыто на публичной) + **#7-B инлайн-цена**
   (поповер, Decimal, ТОЛЬКО без вариантов, bump кэша). Коммиты `a0c0ec7`/`694531d`. (Уровень C — инлайн-фото —
   = M4 ниже.)

## МЕДИА-ТРЕК (попап-редактирование лого/слайдера/фото — запрос владельца «как Битрикс/Тильда»)
Разведка: лого не выводилось, слайдер `heroes[]` не редактировался в билдере. Порядок M1→#7AB→M2→M3→M4.
Принцип владельца: **«как Лого» = загрузка ФАЙЛОМ из панели/области (не on-canvas-клик).**
- ✅ **M1 — лого** (`Tenant.logo_url` в шапке `_base.html`; билдер Theme→Logo → область `logo-media`).
- ✅ **M2 — слайды баннера** (`site_config["heroes"]`, ≤6; область `banner-media` + кнопка «Manage slides» в
  🖼 Banner; на слайд фото-файл+заголовок+текст+кнопка; save/delete/move_hero_slide). Витрина-слайдер уже был.
- ✅ **M3 — обложки разделов из билдера:** область `covers-media` (кнопка «Cover photos» в 🖼 Banner), цикл
  по `cover_specs`, реюз `_upload_cover_hero` (folder=cover), экшен в `home_builder_view`. Загрузка cover
  «catalog» проверена (archetypes.catalog.hero_image). Интро/галереи разделов остались на странице Sections.
- ✅ **M4 — инлайн-замена фото товара:** кнопка 📷 на карточке (режим правки) → файл-диалог → multipart
  `catalog:product-photo-edit` → новое фото primary + bump кэша. Проверено (DB images[0] primary, карточка).
- Заметка: on-canvas клик по баннеру НЕ сделали (баннер обёрнут в `[data-sf-section=hero]`, секция-хендлер
  перебивал на «sections»). «Как Лого» этого и не требует — открываем кнопкой.
- **ВЕСЬ МЕДИА-ТРЕК ГОТОВ** (M1 лого / M2 слайдер / M3 обложки / M4 фото товара) + #7 A+B. Дальнейшее (опц.):
  per-архетип галерея на канве; on-canvas клик-открытие медиа-областей; EN-локаль для inline-текста.

### Тестовый стенд (как поднять в новой сессии — стенд эфемерный, пересоздаётся)
`migrate_schemas --shared` → `create_test_tenant` → seed (юзер `owner@test.de`/`test12345` + 6 товаров +
форс-логин сессия в Redis: скрипт-образец был в scratchpad, см. шаги ниже) → `runserver 0.0.0.0:8000`.
Браузер: Chromium с `--host-resolver-rules=MAP *.siteadaptor.de 127.0.0.1`, cookie `sessionid`, навигация на
`http://baeckerei-test.siteadaptor.de:8000/dashboard/site/home/`. ЛОКАЛЬНО отключить debug-toolbar
(`INTERNAL_IPS=[]`, иначе `NoReverseMatch 'djdt'` на `/dashboard/`). Для кабинета (`/dashboard/`) сначала
`onboarding.advance(tenant, skip=True)` (иначе редирект на `/dashboard/setup/`).
**Грабли сессии 2 (учти):** (1) для просмотра фото/лого/слайдов в dev включи `SERVE_MEDIA=True` в
dev-override (иначе `/media/` 404 → битая картинка + alt-текст). (2) Сессии лежат в Redis-cache —
`cache.clear()` ВЫНОСИТ логин-сессии (пере-сидируй). (3) Публичная витрина кэшируется (SE-5a) — правки шаблонов
видны сразу в `?preview=1`, на публичной — после bump/экспирации. (4) `runserver` запускай `--noreload` и
рестартуй вручную при правке .py; убийство процессов по PID (паттерн `pkill -f runserver` само-убивает шелл).
При «port in use» — просто бери следующий порт (8001/8002…) и наводи Playwright туда.

## Не путать / отложено
- **«Готовые виды» (галерея обликов) — НЕ ТЗ** (была опечатка «моб[ильную] версию»; мобильное превью уже сделано).
- **SE-9c-2 «очищенный мастер» — ОТЛОЖЕН** (конфликт с SE-9g; Простой режим уже де-загромождает).

## Доки
`docs/storefront-onsite-editor-plan.md` (план редактора SE-1..SE-9) · `docs/storefront-inline-content-plan.md` (Фаза 1-3)
· `docs/build-log.md` (хронология) · `CLAUDE.md` (память проекта, роадмап §7).
