
## Связь с CLAUDE.md (память проекта — авто-грузится в новой сессии)
Этот хендофф = ДЕЛЬТА текущей сессии (редактор витрины + inline-content), НЕ замена CLAUDE.md.
CLAUDE.md даёт: §1-2 что это/архитектура (Django 5.1 multi-tenant DACH, apps/*), §5 конвенции
(CI, --reuse-db, батч-режим), §6 доки, §7 БОЛЬШОЙ роадмап, §8 деплой (Hetzner, deploy.sh single).
**Текущий активный под-трек = редактор/inline-content (этот документ).** После него — по CLAUDE.md §7:
M20 Site Builder, наполнение архетипов «Спринт F» (A6 онлайн-события/A4 диет-фильтр/A3 карточка услуги/
A7 before-after/A8 фасеты/A1-2 отзывы о товаре), Спринт G «анти-Битрикс» (AB1-5: меню по задачам/страница
«Модули»/мастер онбординга — частично сделано), Stage 2/3. Source of truth по сделанному — `docs/build-log.md`.

## Точное состояние git (на конец сессии)
- Ветка `claude/event-detail-section-order-qk6t76`. Всё из раздела «Сделано» выше — уже в **main** (деплоено-готово).
- **НЕ в main, только на ветке:** фикс баг D (раскладка) + тесты + этот SESSION-HANDOFF.
  Что сделать новой сессии: ПРОВЕРИТЬ баг D в браузере → push → CI зелёный → FF-merge в main → деплой.
- Если правил миграции — `pytest` с `--create-db`; иначе `--reuse-db`. Здесь миграций НЕ добавляли.

## Не путать / отложено (чтобы не воскрешать ошибки)
- **«Готовые виды» (галерея целых обликов) — НЕ актуальное ТЗ.** Это была опечатка владельца («моб[ильную]
  версию»), уже реализована мобильная версия превью. Не делать без явной просьбы.
- **SE-9c-2 «очищенный мастер» (список только вкл/порядок) — ОТЛОЖЕН:** конфликтует с SE-9g (правка моб-
  раскладки в мастере) и Простой режим уже де-загромождает. Не делать без явной просьбы.
- **AskUserQuestion в этом окружении не срабатывал** — развилки уточнять обычным текстом.
- **CI-проверка:** `mcp__github__actions_list` отдаёт ~400КБ (уходит в файл) → парсить `jq`, файл удалять.
- **Прерывания:** длинные операции запускать в ФОНЕ (`run_in_background`) — обычные команды владелец прерывает
  сообщениями, и результат теряется. И писать простым текстом на каждом шаге.
