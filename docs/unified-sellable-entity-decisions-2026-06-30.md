# Единый слой продаваемой сущности — ЧЕК-ЛИСТ ОТКРЫТЫХ РЕШЕНИЙ (согласование до кода) — 2026-06-30

> Сводит открытые решения ВСЕХ фаз мастер-трека `docs/unified-sellable-entity-master-track-2026-06-30.md`
> (детальные планы U-A…U-E — `…-ua/ub/uc/ud/ue-plan-2026-06-30.md`). Цель — зафиксировать развилки
> ДО реализации. Легенда воздействия: 🔴 миграция · 🟠 внешнее/стоимость/DSGVO · 🟡 объём/scope ·
> 🟢 безопасный дефолт (без миграции). «Реком.» — рекомендация из плана.
> **Статус:** ⬜ открыто · ✅ зафиксировано.

## A. Уже зафиксировано (U-A, 2026-06-30) — справочно

| # | Решение | Выбор |
|---|---|---|
| A-1 | Форма детали услуги | ✅ **СПЛИТ** (SEO-страница описания + слот-пикер под-страницей брони) |
| A-2 | A7/A9 primary-CTA (booking↔request) | ✅ **per-tenant/service override** (`Service.primary_action`) |
| A-3 | Отзывы per-item | ✅ **generic `Review`-модель сразу** + data-migration из `ProductReview` (UA4-4a) |

> Это снимает и **U-C D2** (где живёт generic Review) — уже в U-A (UA4-4a). Подтверждения не требует.

## B. Требуют твоего решения (развилочные — scope/миграция/стоимость)

| # | Фаза·подзадача | Вопрос | Варианты (реком. первым) | Воздействие |
|---|---|---|---|---|
| **B-1** | U-E · UE1-\* | **Канва акций: как далеко.** | (a) **composable-slots** — промо-блок с пресет-позициями бейджа, overlay-текст поверх фона, кнопка, богатые стиль-контролы, на существующем стек+visual+`collect()` *(реком.)* · (b) **настоящая свободная канва x/y/z/w/h** (пиксельный drag/resize/наложение — новая координатная схема, absolute-рендер, новый JS-стек) | 🟡 головное; (b) резко больше объём + a11y/мобайл/purge-риски (редактор сознательно НЕ drag-and-drop) |
| **B-2** | U-D · UD4-1 | **SMS-канал: сейчас или отложить + провайдер.** | (a) **сейчас**, за фичефлагом дефолт-выкл, opt-in — закрывает T8/E-8 · (b) **отложить** (поднять вместе с whatsapp одной волной). Если (a) — провайдер: **Twilio** / MessageBird / Vonage | 🟠 внешняя интеграция: per-message стоимость, opt-in обязателен (DSGVO), креды per-tenant |
| **B-3** | U-B · UB3-2 | **Группировка услуг/номеров (каталог Leistungen).** | (a) **плоское поле `category`** (как `events`, миграция на `Service`/`StayUnit`) *(реком.)* · (b) generic-коллекция M2M (крупнее) · (c) оставить flat (без группировки) | 🔴 (a)/(b) — миграция; (c) — без |
| **B-4** | U-C · UC1-2/UC2-3 | **Правовые/инфо-страницы (AGB/Impressum/Datenschutz/Widerruf) — first-class редактируемые page-type СЕЙЧАС?** | (a) **сейчас** — реестр делает почти бесплатно, тянет вперёд E-2 (правовой пакет) *(реком.)* · (b) позже (оставить text-fallback, поднять в E-2) | 🟡 scope (тянет E-2 в волну U-C) |

## C. Дефолты (иду по «реком.», если не возразишь)

| # | Фаза·подзадача | Вопрос | Реком. дефолт | Альтернатива | Возд. |
|---|---|---|---|---|---|
| **C-1** | U-C D1 + U-E D3 (**связаны**) | Инлайн-правка: один диспетчер или 6 эндпоинтов | **Один диспетчер `sellable-inline-edit` за адаптером, 6 URL — тонкие алиасы** (JS не трогаем; промо — свой view сейчас, мигрирует в диспетчер позже). **Никогда не править `status`/`available_quantity`** (анти-оверселл) | Полная замена URL (риск) · оставить 6 | 🟢 |
| **C-2** | U-C D4 | Галерея услуги (Service одиночное фото) | **Шим dict→[dict] сейчас (без миграции, replace-only)**, поле-список — позже при нужде reorder | Сразу миграция `Service.image`→список | 🟢 (🔴 если сразу) |
| **C-3** | U-B · UB2-2 | Бэкенд поиска на витрине | **`icontains` v1** (per-tenant, дёшево) | Сразу Postgres FTS/trigram | 🟢 |
| **C-4** | U-B · UB1-2 | Охват унифиц. карточки | **Листинг + home-секции сразу** (макс. переиспользование) | Только листинг, секции позже | 🟢 |
| **C-5** | U-D D1 | Склад-леджер: модель истины | **Append-only лог РЯДОМ со счётчиком** (счётчик — истина, реконсиляция-вью; не трогает 5 движков) | Счётчик = `sum(delta)` (переписывает горячий путь) | 🟢 (🔴 таблица `StockMovement` в любом случае) |
| **C-6** | U-D D2 | Кабинет заказов | **Per-app страницы + общая Kanban-доска `/dashboard/board/`** (де-риск) | Единый inbox-маршрут заменяет per-app | 🟢 |
| **C-7** | U-D D4 | Унификация line-items | **НЕ унифицировать** — проекция отдаёт `title`+`subtotal_display` (избегаем int/Decimal-конфликта OrderItem↔JobLine) | read-only `LineItem`-протокол · полная унификация модели | 🟢 |
| **C-8** | U-E D2 | Промо-блок: данные | **LIVE (`promo_pk`, тянет из БД)** с fail-safe скрытием — цена/остаток/countdown актуальны, JSON-LD валиден | Статический клон (риск устаревшей цены, без JSON-LD) | 🟢 |
| **C-9** | U-E D4 | Промо-шаблоны | **Через `block_templates` (`key='promo'`)** — реюз `normalize_block_templates`/SE-4a-UI/инсертера | Новый реестр `promo_templates` | 🟢 |

## D. Порядок реализации (после согласования)

Мастер-порядок волн: **U1 (U-A)** → **U2 (U-B → U-C)** → **U3 (U-D)** → **U4 (U-E)**; E-2/E-6/E-7/E-8 —
параллельно/после. Ближайший старт кода — **UA1-2** (регистрация `Service`-детали в `DETAIL_ENTITIES`
+ превью редактора), затем волна U-A по `…-ua-plan…`. B-4 (правовые) тянет E-2 в U-C, если (a).

## E. Связанные
`…-master-track…` · `…-ua/ub/uc/ud/ue-plan-2026-06-30.md` (секции «Открытые решения» каждого) ·
`docs/market-gap-synthesis-2026-06-30.md` (эпики E-1…E-15).

---
_После согласования: проставить выбор в колонках «Статус», перенести зафиксированное в §A-аналог
каждого плана, и стартовать кодом строго по планам._
