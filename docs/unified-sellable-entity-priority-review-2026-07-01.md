# Ревью структуры плана U-A…U-E + гэп-анализ конкурентов + приоритеты — 2026-07-01

> Запрос владельца: пройтись по всему детализированному плану + анализу недостатков против конкурентов,
> посмотреть структуру/зависимости для повышения приоритета; проверить мультиязычность (не увидел
> полноценного модуля). Разбор — по 3 ревью-агентам (покрытие эпиков · статус языкового модуля ·
> кросс-волновые зависимости), всё против кода/доков. Источники: `market-gap-synthesis`,
> `archetype-completeness-audit §6`, планы U-A…U-E, `…-decisions`.

## 1. Покрытие конкурентных гэпов (эпики E-1…E-15) волнами U-*

| Эпик | Tier | Покрытие | Волна | Пробел |
|---|---|---|---|---|
| **E-1** деталь услуги + Leistungen | 1 | ✅ полно | U-A (UA1-1/1-2/4-3) | — |
| **E-2** правовой пакет DACH (AGB/§312j/PAngV/засев) | 1 | ⚠️ частично | U-C (UC1-2/2-3 page-type) | **AGB-поле/маршрут/ЗАСЕВ текстов не расписаны** |
| **E-3** JSON-LD по архетипу | 1 | ✅ полно | U-C4 (UC4-2) | — |
| **E-4** отзывы per-item | 1 | ✅ полно | U-A4+U-C4 (UA4-4a/b, UC4-1) | — |
| **E-5** reuse-пачка движков | 1 | ✅ полно | встроено в адаптеры U-A/B/D | — |
| **E-6** ЯЗЫКОВОЙ МОДУЛЬ (L1–L4) | 2·**HIGH** | ❌ **НОЛЬ в волнах** | — (только «параллельно/после») | **не детальная волна; и это ЗАВИСИМОСТЬ — §2** |
| **E-7** платёжный микс DACH | 2·**HIGH** | ⚠️ частично | U-D1 (только упоминание поля) | **`Order.payment_method`/пикер/интеграция не расписаны** |
| **E-8** SMS/WhatsApp | 2 | ⏸️ отложено | снят из U-D (B-2) | по решению — позже с WhatsApp |
| **E-9…E-12, E-15** вертикали A9/A4/A8/A5/A3 | 3 | ❌ не в волнах | — | вертикальные, поверх готового слоя (ожидаемо) |
| **E-13** A6 per-attendee | 3 | ⚠️ частично | reuse G4 в U-A | roster/ics не расписаны |
| **E-14** A1/A2 поиск+каталог | 3 | ⚠️ частично | U-B2 (UB2-2/2-3) | CSV/DHL/wishlist не расписаны |

**Вывод:** Tier-1 закрыт (E-1/E-3/E-4/E-5), кроме **E-2 (засев правового)**. Из Tier-2 HIGH — **E-6 (язык)
не покрыт вовсе**, **E-7 (платежи) только полем**. Это три недо-покрытых сквозных гэпа против конкурентов.

## 2. Мультиязычность — НЕ пропущена тобой: её действительно нет как волны

**Где она сейчас (только это):**
- Эпик **E-6** в `market-gap-synthesis` (Tier 2, «параллельно/после», L1–L4).
- План **L1–L6** — в `archetype-completeness-audit-2026-06-30.md §6.4` (стр. 270–287), НЕ как детальная волна.
- Ссылка в мастер-треке `…master-track:118,131`: «E-6 языковой модуль (L1–L4) — I18nMixin на stays/events
  ложится в адаптер», порядок — «Параллельно/после» волн.

**Текущий статус в коде (верифицировано):**
- ✅ **Есть фундамент:** `I18nMixin` (`core/models.py:31-52`); i18n-JSONField на Product/Category/Event/
  Promotion; переключатель DE/EN на витрине тенанта (`urls_tenant.py:133` + `_base.html:87-90` +
  `set_language` `public_views.py:445-452`); оверлей `localize`/`_deep_overlay` (`siteconfig.py:1006-1020`).
- ❌ **Сломано/нет:** `Tenant.enabled_locales`/`default_locale` (`models.py:32-33`) **не читаются в рантайме**
  (`OVERLAY_LOCALES=("en",)` захардкожен, `siteconfig.py:974`); `.po/.mo` **пусты** (`/locale/` = `.gitkeep`)
  → хром на EN показывает немецкий; **хром/письма/правовое — DE-only**; **`Service`(A3/A7/A9) и `StayUnit`(A5)
  вообще без i18n-полей**; правовое не засеяно; на **публичном домене переключателя нет**; кабинетного UI
  языков нет; EN-контент только у `pranasy`.
- **План достройки L1–L6:** L1 рантайм-биндинг локалей (S, без миграции) · L2 кабинет «Sprachen» (M) ·
  L3 EN-контент в формы/во все киты + **i18n-поля на Service/Stay (миграция)** (M) · L4 хром/письма/`.po/.mo`
  (M) · L5 правовое мультиязычное (M) · L6 язык в URL (`/de/`, `/en/`, опц., отложено).

**⚠️ ПОЧЕМУ ЭТО ЗАВИСИМОСТЬ, А НЕ «ПОСЛЕ»:** контракт `SellableEntity` (UA1-3: `title_i18n`/`description_i18n`/
`attributes`) и UA4-3 (атрибуты/FAQ услуги) и диспетчер инлайна UC2-4 (документированная «i18n-асимметрия:
service — плоские строки») **запекают DE-only для Service/Stay**. Если язык-модуль (конкретно **L3 = i18n-поля
Service/Stay**) делать ПОСЛЕ U-A, то:
- адаптер несёт Service/Stay плоскими DE-строками → при добавлении i18n позже **рефактор адаптера + инлайн-
  диспетчера + детальных шаблонов**;
- **A3/A7/A9 и A5 двуязычной детали не будет** (E-1 частично обесценивается для не-DE);
- демо-киты (кроме pranasy) остаются одноязычными — «выглядит недоделанным».

## 3. Приоритетные правки (по зависимостям, ранжировано)

| P | Правка | Почему (зависимость/гэп) | Стоимость / выигрыш |
|---|---|---|---|
| **P1** | **Языковой модуль → отдельная детальная Волна L; L1+L2 ДО кода U-A, L3 (i18n Service/Stay) ПАРАЛЛЕЛЬНО U-A** | Адаптер U-A иначе печёт DE-only → рефактор; A3/A5 без двуязычной детали. L1/L2 = ~3-4 дня без миграции | 🟢 низкая / 🔴 высокий |
| **P2** | **E-7 платежи: `Order.payment_method` в scope UD1-1** (адаптер несёт поле) **+ параллельный трек оплаты во время U-D** | Kauf-auf-Rechnung/Klarna/SEPA — топ-гэп DACH (критичен A7/A9); ретрофит после U-D по готовой проекции заказа — грязный | 🟠 средняя (координация) / 🔴 высокий |
| **P3** | **E-2 правовой засев — явный инкремент в U-C** (AGB-поле+маршрут+футер + засев Impressum/Datenschutz/Widerruf/AGB во все 9 китов) | B-4 втянул правовые как page-type, но контент/поле/засев не расписаны; демо показывает placeholder | 🟢 низкая / 🟡 средний |
| **P4** | **UA4-4a (generic Review + data-migration) — параллельно UA4-3** | Блокирует UB2-3 (рейтинг-фасет) и UC4-1 (блок отзывов); разные приложения → без контеншена | 🟢 низкая / 🔴 высокий |
| **P5** | **UB3-2 Collection — мини-разведка модели ДО UB3-1** | B-3=M2M поднял UB3-2 до L+миграция; нельзя бэкфиллить Collection в готовые запросы Service/Stay | 🟢 низкая / 🟡 средний |
| **P6** | **UD3-1 (`StockMovement`-схема) — проектировать параллельно UD1-1** | Единственная новая таблица U-D; correctness-critical (не задвоить, атомарность orders/jobs); миграция на tenant-схемах нужен запас | 🟢 низкая / 🟡 средний |
| **P7** | **UD4-2 vs A9 SMS — ревизия** | B-2 отложил SMS; A9 Werkstatt repair-статус ждёт SMS-канал; без него — email-only дефолт | 🟢 низкая / 🟡 средний (мессадж) |

**Кросс-волновой критпуть (реальный):** `UA1-3 → UA2-1 → UA4-1 → UC1-1 → UC2-1 → UC2-2 → UE1-3 → UE4-2`.
Фундаменты с высоким fan-in: **UA1-3** (контракт), **UA4-1** (реестр детали → кормит UC1-1), **UC1-1**
(PAGE_SECTION_REGISTRY), **UC2-1** (draft-схема), **UD1-1** (Transaction). i18n-фундамент (L1-L3) — сбоку,
но **пересекает UA1-3** (адаптер) → должен идти впереди/вместе.

## 4. Предлагаемый скорректированный порядок

```
Волна L (язык, НОВОЕ):  L1 → L2   ─── ДО U-A ── (рантайм-локали + кабинет, без миграции)
                        L3 (i18n Service/Stay + миграция) ─ ПАРАЛЛЕЛЬНО U-A ─ (адаптер сразу locale-clean)
U1 (U-A):  UA1-1 → UA1-2 → UA1-3 → UA2-1 → UA3-* → {UA4-3 ∥ UA4-4a} → UA4-4b
U2:  U-B (UB3-2 мини-разведка Collection до UB3-1) → U-C (E-2-засев внутри/сразу после UC1-2)
U3 (U-D):  UD1-1 (+ поле Order.payment_method) ∥ UD3-1-схема ; параллельный трек E-7 оплата
U4 (U-E):  как есть (slots-first)
После:  L4 (хром/письма) → L5 (правовое i18n, с E-2) → L6 (URL, опц.) ; E-8 SMS+WhatsApp ; вертикали E-9…E-15
```

## 5. Развилки для владельца (вынесены на согласование)
- **PR-1 — Мультиязычность:** поднять в отдельную детальную **Волну L** и L1-L3 до/параллельно U-A?
  (реком. да) · оставить как E-6 после волн · только «защитить адаптер» (lazy-locale в UA1-3), полный модуль позже.
- **PR-2 — E-7 платежи DACH:** `Order.payment_method` в UD1-1 + параллельный трек оплаты во время U-D
  (реком.) · оставить после всех волн.
- **PR-3 — Детальный план Волны L (L1-L6):** написать сейчас (как U-A…U-E) · после решения по приоритету.

## 6. Связанные
`docs/market-gap-synthesis-2026-06-30.md` (E-1…E-15) · `docs/archetype-completeness-audit-2026-06-30.md §6.4`
(план L1–L6) · `docs/unified-sellable-entity-master-track-2026-06-30.md` (§U-F E-6/E-7, §4 порядок) ·
`…-ua-plan…` (UA1-3 адаптер, i18n-асимметрия) · `…-uc-plan…` (UC2-4 i18n fail-closed) · `…-decisions-…`.
