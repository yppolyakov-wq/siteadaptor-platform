# Полное покрытие переводом: кабинет + витрина + демо (2026-07-30)

Запрос владельца: «все языки во всех уголках админки и демосайтов, чтоб всё было
переведено, включая характеристики и вспомогательные надписи, кнопки и т. д.»

## 0. Что показала разведка (3 параллельных аудита)

**Каталоги `.po` формально полны:** en/tr/ru/uk — по 3168 записей, 0 пустых,
0 fuzzy, наборы msgid идентичны, плейсхолдеры целы. **de.po СУЩЕСТВУЕТ** (в git,
3132 → досинхронизирован до 3168). Значит проблема НЕ в переводах, а в том, что
**строки до `.po` не доезжают**.

**Сырые строки вне `gettext` — ~1100 в ~170 файлах:**

| Слой | Кол-во | Где именно |
|---|---|---|
| `choices=` / CHOICES-константы моделей | ~292 | orders/booking/jobs/inventory/events/promotions/stays/tenants/catalog |
| Реестры-справочники | ~290 | `tenants/siteconfig.py` (85), `tenants/sitetemplates.py` (42), `core/finder.py` (57), `promotions/presets.py` (35), `core/setup_steps.py` (26), `core/page_presets.py` (18), `core/admin_dashboard.py` (12), `account/account_data.py` (11) |
| forms.py label/help_text/placeholder | ~45 | events (25+7), catalog, promotions, imports |
| `messages.*` | 21 | `promotions/public_views.py` (waitlist — регрессия: соседние функции обёрнуты), `promotions/views.py`, `aggregator/featuring.py` |
| views-контекст (заголовки/кнопки/CSV) | ~60 | `core/views.py`, `promotions/views.py` (FSM-кнопки — ещё и по-английски), events/stays/inventory/aggregator |
| PDF / письма / постеры | ~100 | jobs/finance/orders/events/promotions |
| **Шаблоны кабинета** | 73 явных + 33 пограничных + 10 в JS | `promotion_feature.html` (11), `listing_feature.html` (9), `platform_bi.html` (9), `_section_fields.html` (6), `aggregator/_cards.html` (4), `combo_form.html` (4), `finder_settings.html` (4), `billing/payments.html` (3), `_setup_buttons.html` (3), `coupon_campaigns.html` (3), `site_menu.html` (7 в JS) |

**Отдельно найденные дефекты (не «непереведённость», а поломки):**
- `%%` в msgstr 4 локалей для `Lücken-Deal: Rabatt %` → рендерился бы буквально. **Исправлено.**
- de.po отставал на 37 msgid. **Досинхронизирован.**
- 3 немецких хвоста в en (`extras.html` ×2 «Übernachtung», jobs «TÜV» — реалия, оставляем).
- Заголовки tr/ru/uk/de — заглушки msginit (`Language: ''`). Косметика, Django берёт локаль из пути.
- Точечные смысловые ошибки ru/uk/tr: «Artikel»→«статья» (надо «товар»), «Meldebestand»→«значение
  в отчёте», «geschwärzt»→«зачеркнуто», род в «Этот бронирование», tr «Değer» вместо «Miktar».

## 1. Порядок волн (по видимости для владельца)

- **I18N-1 — статусы и справочники моделей** (~292). Самое видное: списки заказов,
  доска, панель переименования статусов, формы. Осторожно: `choices` в миграциях —
  оборачивать ТОЛЬКО метки, миграции не переписывать (метки схему не меняют, см.
  урок 2026-07-13 про StayBooking/ServiceBooking).
- **I18N-2 — конструктор сайта/Studio** (~145: siteconfig + sitetemplates + page_presets).
  Владелец на ru/uk/tr видит сплошной немецкий в самом посещаемом разделе.
- **I18N-3 — шаблоны кабинета** (73 A-класса + 10 JS). Начать с
  `promotion_feature`/`listing_feature` (20 строк, почти идентичны — msgid переиспользуются).
- **I18N-4 — формы и мастер** (events/forms 25, registration 7, setup_steps 26, imports 46).
- **I18N-5 — флеш-сообщения и views-контекст** (21 + ~60), включая FSM-кнопки акций
  (сейчас английские при немецкой базе — двойная несогласованность).
- **I18N-6 — витринный Finder** (57): единственный крупный блок покупательского UX вне `.po`.
- **I18N-7 — PDF/письма/постеры** (~100). Отдельно: письма уже имеют `_render(locale)`
  (L4), поэтому выигрыш реальный.
- **I18N-8 — качество**: смысловые ошибки ru/uk/tr из аудита + заголовки .po.

## 2. Правила исполнения
- Базовый msgid — как в файле (в проекте смешаны немецкие и английские msgid;
  НЕ переписываем msgid ради единообразия — это оторвёт существующие переводы).
- Каждая волна: обернуть → добавить msgid во ВСЕ 5 `.po` (en/tr/ru/uk/de; для de
  msgstr = msgid, если msgid немецкий) → локальные гейты → CI → merge.
- `gettext_lazy` в реестрах/choices (импортируются на старте), `gettext` во вьюхах.
- Ловушка: lazy-строка в JSON `site_config` не сериализуется — в реестрах, чьи
  значения ПИШУТСЯ в site_config, оборачивать только UI-метки, не хранимые значения.
- Ловушка: golden-тесты normalize сравнивают строки — проверять после каждой волны.
- `%` в `{% trans %}` НЕ удваивать (Django не делает %-интерполяцию в trans без
  переменных) — иначе на экране `%%`.


## 3. Статус волн (обновляется по мере выполнения)

| Волна | Статус | Объём |
|---|---|---|
| I18N-1 статусы/справочники моделей | ✅ | 216 меток, 22 файла, +107 msgid |
| I18N-2 конструктор сайта/Studio | ✅ | 76 меток, +69 msgid |
| I18N-3 шаблоны кабинета | ✅ | 7 файлов, +17 msgid |
| I18N-4 формы событий + мастер импорта | ✅ | 62 метки, +55 msgid |
| I18N-5 флеш-сообщения | ✅ | 21 строка, +16 msgid |
| I18N-6 витринный Finder | ✅ | 44 подписи, +44 msgid |
| I18N-8 качество переводов | ✅ | 14 смысловых правок |
| **I18N-7 PDF / письма / постеры** | ⏸ **за решением владельца** | ~100 строк |

### Почему I18N-7 отложен
Это не UI, а ДОКУМЕНТЫ: счета (`finance/pdf.py`), сметы (`jobs/pdf.py`), накладные
(`orders/pdf.py`), билеты (`events/memo.py`), постеры (`promotions/poster.py`).
Развилка, которую нельзя решить за владельца:
1. **Язык документа = язык клиента** (турецкий гость получает счёт по-турецки) —
   удобно, но для DACH счёт/Rechnung обычно оформляется по-немецки, а DATEV-экспорт
   имеет фиксированные немецкие заголовки (их переводить НЕЛЬЗЯ — сломается импорт
   в бухгалтерию).
2. **Язык документа = язык бизнеса** (всегда немецкий) — юридически безопасно,
   но иностранный клиент не понимает счёт.
3. **Гибрид**: маркетинговое (постер, билет, memo) — на языке клиента; финансовое
   (Rechnung, DATEV, Widerrufsbelehrung) — на языке бизнеса.
Рекомендация была — вариант 3. **Решение владельца 2026-07-31: «полный»** (вариант 1) — документы переводятся целиком. Сделано: см.
`docs/i18n-7b-documents-plan-2026-07-31.md` и build-log. Исключения, которые
остались немецкими по технической необходимости: DATEV-экспорт (машинный
формат) и идентификаторы `USt-IdNr.`/`Steuernummer`/§ 19 UStG.

### Правило для новых строк
Любая новая видимая строка добавляется СРАЗУ во все 5 `.po` (`de` включительно;
для немецкого msgid — `msgstr = msgid`). Локально после правки `.po` обязательно
`msgfmt -o …django.mo …django.po` по всем локалям — иначе переводы в тестах не
активны и класс «замок пинит сырой литерал» всплывёт только на CI.

### I18N-9 (2026-07-31): почему «обёрнуто» ≠ «переведено»
Стенд кабинета демо `mode` на ru показал немецкие «Sichtbar»/«Bearbeiten» —
хотя строки давно были в `{% trans %}`. Причина системная: **`makemessages`
падал** (`xgettext: msgid 'Only %(n)s left' is used without plural and with
plural` — `_buybox.html` брал строку `blocktrans with`, акции `blocktrans
count`), поэтому `.po` велись руками и всё, что забыли вписать, молча
выводилось по-немецки. Побочно singular-форма вообще не находила запись —
строка не переводилась даже на de.

Сделано: конфликт снят (buybox → count-форма), сверка кода с `.po` дала **401**
непокрытый msgid, из них **177 UI переведены на 5 локалей**. Регрессию ловит
`scripts/i18n_gap.py` + шаг CI «i18n coverage (I18N-9)».

### Открытое решение владельца: Branchen-лендинги (224 строки)
Оставшиеся 224 msgid — маркетинговая копия `apps/tenants/archetype_pages.py`
(hero/описания/фичи 14 архетипов). Это SEO-текст под DACH-рынок: перевод —
вопрос тона и ключевых слов, а не техдолг. Сейчас — в allowlist гейта
(немецкий на всех языках). Варианты:
1. **Оставить немецкими** (сейчас): целевой рынок читает по-немецки, SEO цел.
2. **Перевести на en** (иностранные владельцы бизнеса в DACH — турки/русские/
   украинцы часто ищут по-английски) — средний объём, ощутимая польза.
3. **Перевести на все 4** — самый дорогой вариант; ru/uk/tr-лендинги без
   локального SEO дадут мало трафика, но снимут «немецкую стену» на входе.
Рекомендация: вариант 2, затем при спросе — tr/ru/uk.
