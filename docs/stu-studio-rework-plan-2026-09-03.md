# STU — переработка Студии: два уровня настройки + охват «всем / только здесь»

**Дата:** 2026-09-03 · **Статус:** ВОЛНА ИСПОЛНЕНА (STU-1..6, вариант A) ·
**Запрос владельца:** дословно в §1 · **Разведка:** §2 (факты с путями) ·
**Варианты на утверждение:** §4 · **Развилки:** §6

---

## 1. Запрос владельца (дословно)

> «Нужно пересмотреть студию. Есть 2 уровня: настройка шаблона/дизайна сайта общая,
> и настройка макета каждого типа страниц — главная, категория, категория акции,
> вложенная категория, страница товара, страница акции, текстовые страницы, корзина,
> оформление заказа и т.д. Настройка каждого типа должна включать настройку макета,
> блоков там, где это необходимо. Если я нахожусь на странице товара, то показывает
> настройки именно этого типа в зависимости от типа, и мы ещё говорили о том, что
> применить для всех или изменить только для этого товара или категории — это как раз
> нужно в студии, а то сейчас там каша и непонятно, что и где настраивается. Отдельная
> настройка слева "Общий макет", и далее уже детально об этой. И при клике в этой
> области, как на главной или категории, так же должно быть на остальных типах
> страниц: открытие лайаута и вариации отображения контента в этом шаблоне (могут быть
> наборы). Старые настройки дизайнов вроде уже не актуальны, они как-то мешают только,
> давай подумаем. Давай обсудим и сделаем вначале дизайн студии, затем утвердим,
> предложи варианты.»

---

## 2. Что есть сегодня (разведка, факты)

Редактор — `templates/tenant/site_home.html` (3779 строк), вьюха
`apps/core/views.py::home_builder_view` (стр. 1274), маршрут `site-home`.

### 2.1 Каркас

| Элемент | Где | Что делает |
|---|---|---|
| Верхний тулбар | site_home.html:9–78 | Undo/Redo, контекст блока, device, ☰Menu, 🧱Blocks, ⚙️Template, Save |
| Рейка `#st-rail` | 104–111 | 5 кнопок `data-st-level`: look / pages / blocks / media / quickstart |
| Лента страниц `#st-pages` | 114–118 | чипы `preview_pages`, клик → `?page=<url>` |
| Правый инспектор | 128+ | вкладки `#bld-area-tabs`: theme / banner / menu / footer / library |
| Лента настроек блока | 515–518 | строка формы ФИЗИЧЕСКИ переносится в попап и возвращается |
| Инсертер «+» | 1426, 2620–2840 | двухшаговый (тип → вариант), `page_key = curPbHost` |

**Рейка ≠ области.** `st-level` — тонкая обёртка над вкладками: `look` → клик по вкладке
«theme», `pages` → показать ленту страниц, `blocks` → открыть инсертер, `media`/`quickstart`
→ `showArea(...)`. То есть **две несогласованные навигации** поверх одной формы.

### 2.2 Три уровня уже существуют В ДАННЫХ — но не в интерфейсе

**(а) Весь сайт** — `site_defaults.*` (card_style, promo_card, card_chrome, card_radius/
shadow/padding/bg, page_bg, media_shape, card_slider, variant_style, category_page_style,
promo_group_style), `theme`, `font`, `typography`, `hero_style`, `nav.*`, `design`
(Look/Bundle), `Tenant.primary_color`, `quick_add`, `wishlist`, галерея, шаблоны блоков.

**(б) Конкретная страница** — `sections[]` + `section_titles` + `heroes` (главная) ·
`page_blocks[host]` · `catalog_layout` / `events_index_layout` / `stay_index_layout` /
`service_index_layout` · `catalog_show_filters` / `catalog_subcats_first` / `catalog_sort` /
`catalog_page_style` · `cart_show_upsell` · `product_detail.hidden|layout` ·
`event_detail.order|hidden` · `service_detail.hidden` · `stay_detail.hidden` ·
`promo_layout` / `promo_grouping` / `promo_page_style`.

**(в) Конкретный объект — В СТУДИИ НЕДОСТУПНО ВООБЩЕ.** `Product.card_style`,
`Category.page_style`, `Promotion.card_style`, `site_config["promo_groups"][<группа>]`
правятся только в формах кабинета (`product_form.html:141`, `category_form.html:75`,
`promotion_form.html:108`, `promotion_list.html:78`). Механика «своё бьёт общее» уже
работает (волны DL-19/20/21) — Студия просто не показывает эту ось.

### 2.3 Где клик по канве уже открывает настройки, а где нет

* `data-sf-section` → `openBlockPopup` — **работает** (главная, секции).
* `data-ed-section` — только drag, инспектор НЕ открывает; есть лишь у события и тура.
* **Страница категории** `/sortiment/<slug>/` — блоки есть (`catalog:<slug>`), но панель
  показывает их, только если блоки уже заведены; на пустой категории — заглушка.
* **`/aktionen/`** — ни `data-sf-section`, ни `data-pb-host`, страницы нет в
  `preview_pages`; её настройки лежат в области «Тема» (то есть видны на любой странице
  и не видны на своей).
* **Страница группы акций** — вне Студии целиком.
* **Правовые страницы** — в ленте есть, хоста блоков нет → заглушка; скоуп-фильтр без
  `curPbHost` считает их главной → показывает контролы главной.
* **Детали товара/услуги/номера** — только чекбоксы видимости секций (+ `pd_layout`).

### 2.4 Дубли (одно и то же в разных местах)

1. **Look/Bundle — три места:** `/dashboard/design/` (сохраняет сразу, серверно), Студия →
   «Тема» (клиентские, требуют Save), мастер «stil». Вид одинаковый, поведение разное.
2. **Layout-Vorlagen** (`templates_for`) — Студия → «quickstart» и мастер «stil»; их Apply
   переписывает `sections` и конфликтует со сборкой.
3. **Раскладки листингов** пишут ДВА экрана: Студия и `/dashboard/site/pages/`
   (`views.py:2985`), причём Студия сама даёт туда ссылку.
4. **`promo_page_style` / `promo_layout` / `promo_grouping`** — Студия и список акций
   (`promotion_list.html:57`), из-за чего в Студии они под presence-guard.
5. **`nav.style/sticky`** — Студия («Меню») и полный редактор меню `/dashboard/site/menu/`.
6. **`nav_style` объявлен ДВАЖДЫ в одной форме** (area theme:236 и area menu:1012).
7. **Карточка/страница**: сайто-дефолт в Студии и per-object в формах — один и тот же
   пикер `_cardform_picker.html` на разных экранах.

### 2.5 Легаси и мешающее

* `#home-prev-page` в шаблоне НЕТ, а JS 3011–3075 написан под него → мёртвый код,
  стартовая синхронизация скоупа не выполняется никогда.
* `#bld-rail` нет → `toggleRail` — no-op, но вызывается в 5 местах.
* `hero_accent` — поля нет, ветка в `views.py:1947` жива (риск класса W0).
* Carry-forward-костыли `site_defaults` (views:1879) — форма пересобирает словарь целиком.
* Россыпь presence-сентинелов (`pb_present`, `cf_present`, `pd_present`, …) — следствие
  «одна форма на всё»: каждый новый контрол требует своего сентинела.
* Область «footer» — ни одного поля, только внешние ссылки.
* `use_page_template` заменяет ВЕСЬ `sections` независимо от страницы канвы — на
  подстранице кнопка перепишет главную.
* Строка настроек блока физически переносится в попап и возвращается по якорю-комментарию
  (в коде есть фикс от «блоки копились в попапе») — хрупко.

---

## 3. Диагноз одной фразой

Данные УЖЕ трёхуровневые (сайт → тип страницы → объект), а интерфейс — плоский:
две несогласованные навигации, настройки страницы лежат в «Теме», объектный уровень
из Студии недоступен, один и тот же выбор дублируется на трёх экранах. Отсюда «каша».

---

## 4. Утверждённый дизайн — вариант A (решение владельца 2026-09-03)

Канвас на согласование: `docs/design/studio-rework-2026-09-03/` (артефакт «Studio v2 —
zwei Ebenen»). Показаны три варианта; **владелец выбрал A**.

**Что принято (вариант A):**

* Левая рейка = **УРОВНИ**, а не области: `🎨 Дизайн сайта` · `📄 Эта страница` ·
  `🧱 Блоки` · `🖼 Медиа`. Сейчас слева области, не совпадающие с содержимым.
* Пункт «Эта страница» подписан **типом** той страницы, на которой стоит канва, и
  показывает **только её** настройки. Перешёл на другую страницу — панель другая.
* **Охват — у КАЖДОЙ настройки** (это и есть выбор A): пилюля
  `для всех ▾` / `только здесь ▾` рядом с контролом; переопределённые помечены точкой,
  в шапке панели — счётчик «N своих настроек». Владелец может смешивать: макет задать
  всем товарам, галерею — только этому.
* **Клик по области канвы** открывает эту же панель на ЛЮБОМ типе страницы (сегодня —
  только главная); панель показывает и макет типа, и блоки этой области.
* В Студию приходит **объектный уровень**, которого в ней не было: шаблон конкретной
  категории, форма карточки конкретного товара и акции, шаблон группы акций.

**Отклонённые варианты (для истории):** B — один переключатель охвата на всю панель
(пришлось бы переключать туда-сюда, чтобы часть настроек задать всем, а часть одному
объекту); C — три режима сегментом вверху (исчезает отдельный вход «Дизайн сайта» слева,
который владелец просил явно).

## 5. Что предлагается убрать (ответ на «старые настройки мешают»)

| Кандидат | Что сделано (STU-5) |
|---|---|
| Экран `/dashboard/site/pages/` | **снесён**: был вторым писателем раскладок. Три настройки, которых в Студии не было (цены и маркировка меню, сетка «Passt dazu»), сначала перенесены в уровень «Эта страница», затем экран → 302 на Студию, шаблон удалён, оба входа из Студии сняты |
| Вкладка «Footer» | **снята**: своих полей не имела, три указателя переехали в уровень «Дизайн» (`data-stu-footer-links`) |
| Мёртвый JS `#home-prev-page` | **удалён**: элемента нет с UC6-1b, ветка (вкл. «стартовую синхронизацию» скоупа) не выполнялась никогда |
| Экран `/dashboard/design/` | **ОСТАВЛЕН.** Он создан DL-7b как ответ на ту же жалобу («выбор темы терялся среди настроек канвы») и сохраняет выбор СРАЗУ, без Save канвы. Снос вернул бы дефект, закрытый два дня назад |
| Область «quickstart» (Layout-Vorlagen) | **оставлена**: несёт демо-контент и шаблоны витрины; сборки живут в «Дизайне» и не конфликтуют, пока Vorlagen — отдельное действие с early-return |
| Дубль `nav_style` | **дубля нет**: в текущем коде поле объявлено один раз (стр. 1031), остальные вхождения — JS-запросы |
| `#bld-rail` / `toggleRail` | **не мёртвый**: `railNav === null`, но функция ставит `aria-pressed` кнопке тулбара |
| `hero_accent` | **не мёртвый**: поля в шаблонах нет, но ветка — покрытая замками обратная совместимость (`test_w6_theme` требует именно её семантики) |

---

## 6. Решения владельца

* **Р-1 (2026-09-03): вариант A** — охват у каждой настройки. См. §4.
* **Р-2: список §5 «что убрать»** — возражений не поступило; сносим как написано, с
  редиректами, чтобы старые ссылки не ломались.
* **Решаю сам, без вопросов** (владелец делегировал): состав типов страниц
  (главная · каталог · категория · товар · акции · группа акций · акция ·
  услуги/номера/события · корзина · оформление заказа · текстовые · правовые) и что
  открывает клик по области (макет типа + блоки этой области одной панелью).

## 7. Ход работ — ВОЛНА ИСПОЛНЕНА (2026-09-03)

| Шаг | Что вышло |
|---|---|
| **STU-1 ✅** | реестр `apps/core/studio_pages.py`: 22 настройки × 18 типов страниц; тип канвы — штатным резолвером по `config.urls_tenant`, не эвристикой по пути |
| **STU-2 ✅** | рейка = УРОВНИ (Design · тип открытой страницы · Blöcke · Medien); новая область «эта страница», куда переехали настройки из «Темы» и весь набор «Landingpages»; тип витрина сообщает сама (`data-stu-page` на body, по уже разобранному `resolver_match`) |
| **STU-3 ✅** | пилюля охвата «Für alle / Nur hier» у каждой настройки с объектным уровнем; точечные чтение и запись мимо большой формы (`apps/core/studio_scope.py` + два вида) |
| **STU-4 ✅** | клик по содержимому открывает настройки этой страницы на ВСЕХ типах (якоря — каркас листингов KAT-5 + один новый маркер на корне детали) |
| **STU-5 ✅** | снесены экран `/dashboard/site/pages/` (после переноса трёх его настроек), вкладка «Подвал», мёртвый JS `#home-prev-page`; §5 выше переписан по фактам — четыре кандидата оставлены осознанно |
| **STU-6 ✅** | сводный стенд 28/28 по двум демо и всем типам; замок «каждый тип узнаётся по адресу, собранному реверсом из его же url_name»; гарантии умершего экрана перенесены на форму Studio |

**Найдено и починено попутно (класс W6/«каша»):** сохранение билдера стирало обложку,
интро, кнопку и галерею раздела (пересобирало запись архетипа из трёх полей вместо
восьми); панель показывала блоки ГЛАВНОЙ на `/galerie/` и `/kombi/` (эвристика
«нет группы и нет хоста → это главная» — теперь решает тип страницы).

**STU-7 ✅ (2026-09-04, следом):** «ограничение v1» оказалось дефектом шире записанного.
Query срезался в ТРЁХ местах — серверный `_safe_preview_page` (`split("?")[0]`),
`location.pathname` в `instrumentFrame` и скрытое `#bld-page-path`, — поэтому на странице
группы акций (`?gruppe=`):

1. **любая смена настройки уводила канву на общий обзор** `/aktionen/` (живой черновик
   перерисовывал кадр по `previewPath` без параметра) — владелец не видел результата
   настройки, которую только что выбрал;
2. **Save** возвращал канву туда же, а не на настраиваемую группу;
3. **deep-link `?page=`** не мог адресовать группу вовсе.

Починка: реестр `studio_pages.PAGE_QUERY_KEYS` — единственный источник списка
«параметры, ЗАДАЮЩИЕ страницу» (сегодня `gruppe`); санитайзер сохраняет их и
по-прежнему рубит всё остальное (фильтры посетителя не адресуют страницу, а служебные
`preview`/`look`/`bundle` подменили бы вид канвы — stateless-превью ST-1b), фрагмент
отбрасывается всегда, значение капится; клиент получает тот же список из реестра и
несёт его в `previewPath`. Замки: 4 в `test_preview_pages` (в т.ч. адверсариальный —
«каждый ключ whitelist'а действительно меняет тип страницы, иначе список выродится в
сквозной проброс query») + 2 в `test_studio_pages`; проверено на стенде до и после.

**Ограничение v1 (остаётся):** на 404-странице внутри канвы тип неизвестен и панель
показывает прежний набор.

⚠️ Ни один шаг не требует миграций: объектные поля (`Product.card_style`,
`Category.page_style`, `Promotion.card_style`) уже есть.
